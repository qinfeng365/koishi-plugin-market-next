import type { MarketPerformanceSnapshot } from '../shared';
import { type EndpointResult } from './market-internals';
export declare function createMarketResultSnapshot(result: EndpointResult, objects: number): MarketPerformanceSnapshot;
